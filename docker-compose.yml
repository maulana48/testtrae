version: '3.8'

services:
  app:
    build: .
    container_name: burnout-detector
    ports:
      - "8081:8081"
    volumes:
      # Persist the SQLite database
      - ./burnout.db:/app/burnout.db
    restart: unless-stopped
